# ============================================================
# Edgeworks AI Support Portal — Python Dependencies
# Runtime: Python 3.11+ | Framework: FastAPI + Uvicorn
# Stack:   Groq Llama-3.3 70B | Neon PostgreSQL | Qdrant Local
# Updated: 2026-03-17
# ============================================================

# ── Web Framework & Server ──────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
gunicorn==22.0.0
starlette==0.37.2
python-multipart==0.0.9
websockets==13.1
aiofiles
aiohttp==3.11.12

# ── Database (Neon Cloud PostgreSQL) ────────────────────────
SQLAlchemy==2.0.38
alembic>=1.18.0
psycopg2-binary==2.9.11

# ── AI / LLM (Groq — Free Tier) ────────────────────────────
langchain-core==0.3.37
langchain-community==0.3.17
langchain-groq==0.2.4
groq>=0.37.1
tiktoken==0.8.0

# ── Embeddings (Local HuggingFace — Zero Cost) ─────────────
langchain-huggingface>=0.1.2
sentence-transformers>=3.0.0

# ── Vector Store (Qdrant Local — File-Based) ────────────────
qdrant-client==1.7.3
rank-bm25==0.2.2
numpy==1.26.4

# ── Auth & Security ─────────────────────────────────────────
bcrypt==5.0.0
PyJWT==2.8.0
cryptography==46.0.5

# ── HTTP & Networking ───────────────────────────────────────
httpx>=0.28.0
httpx-sse==0.4.3
requests==2.32.5
certifi>=2024.0.0

# ── Document Processing ────────────────────────────────────
beautifulsoup4>=4.12.0
lxml>=5.0.0
Markdown>=3.5.0

# ── Config & Utilities ──────────────────────────────────────
pydantic==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.0.1
PyYAML>=6.0.0
Jinja2==3.1.4
tqdm>=4.60.0
python-dateutil>=2.9.0

# ── Resilience & Rate Limiting ──────────────────────────────
tenacity==8.5.0
slowapi==0.1.9
limits>=5.0.0
backoff>=2.0.0

# ── Caching (Optional — Redis disabled by default) ─────────
redis>=5.0.1
fakeredis>=2.21.0

# ── Observability (Optional) ───────────────────────────────
prometheus-fastapi-instrumentator>=7.0.0

# ── Language Detection ──────────────────────────────────────
langdetect==1.0.9

# ── Google OAuth & Cloud Storage (Optional) ─────────────────
google-auth>=2.20.0
google-cloud-storage>=2.10.0
